{
  "name": "@qbane/sveltekit-template",
  "version": "0.0.6",
  "description": "My opinionated SvelteKit template in pure JavaScript.",
  "author": "qbane",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || true",
    "svelte-kit-sync": "svelte-kit sync && rm -rf node_modules/.tmp",
    "tsc": "tsc -b",
    "check": "npm run svelte-kit-sync && svelte-check --tsconfig ./tsconfigs/tsconfig.app.json",
    "check:watch": "npm run svelte-kit-sync && svelte-check --tsconfig ./tsconfigs/tsconfig.app.json --watch",
    "test:unit": "vitest",
    "test": "npm run test:unit -- --run"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "~5.4.0",
    "@sveltejs/kit": "~2.48.4",
    "@sveltejs/vite-plugin-svelte": "~6.2.1",
    "@types/node": "~22.19.0",
    "svelte": "~5.43.6",
    "svelte-check": "~4.3.4",
    "typescript": "~5.9.3",
    "vite": "~7.2.2",
    "vitest": "~3.2.4"
  },
  "engines": {
    "node": ">=20.0.0 <25.0.0"
  }
}
